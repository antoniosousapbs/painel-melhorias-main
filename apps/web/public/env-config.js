/**
 * Configuração de runtime da aplicação.
 *
 * Em DESENVOLVIMENTO LOCAL, este arquivo deve ficar vazio — o código usa os
 * defaults corretos automaticamente (API_BASE='/api' via proxy do Vite,
 * MSAL_REDIRECT_URI=window.location.origin).
 *
 * Em PRODUÇÃO, o pacote de deploy substitui este arquivo por uma versão com
 * os valores reais do servidor (ver apps/web/env-config.production.js).
 * NÃO defina valores de produção aqui — este arquivo é versionado e também
 * é servido pelo Vite em `npm run dev`.
 */
window._env_ = {};
